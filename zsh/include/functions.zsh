#!/bin/zsh

# Extract most files
extract () {
     if [ -f $1 ] ; then
         case $1 in
             *.tar.bz2)   tar xjf $1        ;;
             *.tar.gz)    tar xzf $1     ;;
             *.bz2)       bunzip2 $1       ;;
             *.rar)       rar x $1     ;;
             *.gz)        gunzip $1     ;;
             *.tar)       tar xf $1        ;;
             *.tbz2)      tar xjf $1      ;;
             *.tgz)       tar xzf $1       ;;
             *.zip)       unzip $1     ;;
             *.Z)         uncompress $1  ;;
             *.7z)        7z x $1    ;;
             *)           echo "'$1' cannot be extracted via extract()" ;;
         esac
     else
         echo "'$1' is not a valid file"
     fi
}

function snode() {
    source $XDG_CONFIG_HOME/zsh/plugin/zsh-nvm/zsh-nvm.zsh
}

# git clone / update
function git_clone_or_update() {
    git clone "$1" "$2" 2>/dev/null && print 'Updated' || (cd "$2"; git pull)
}

